package com.example.vpnapp

/**
 * آدرس عمومی بک‌اند (همون ربات/وب‌اپ روی VPS خودت) — کاربر دیگه لازم نیست
 * این آدرس رو وارد کنه، همینجا برای همیشه ثابت شده.
 */
object AppConfig {
    const val BACKEND_BASE_URL = "https://hector.a2pnc.ir:9443/"
}
