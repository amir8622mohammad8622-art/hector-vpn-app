# ساخت APK فقط با گوشی (بدون کامپیوتر)

این راهنما دقیقاً قدم‌به‌قدم می‌گه چطور از روی خودِ گوشی، کد رو به گیت‌هاب بفرستی
و بذاری گیت‌هاب (رایگان) برات APK بسازه.

## قدم ۱ — نصب Termux
Termux یه ترمینال لینوکسیه که روی اندروید اجرا میشه (نیاز به کامپیوتر نداره).
از **F-Droid** نصبش کن (نسخه گوگل‌پلی قدیمیه و کار نمی‌کنه):
https://f-droid.org/packages/com.termux/

## قدم ۲ — آماده‌سازی Termux
توی Termux این دستورها رو یکی‌یکی بزن:
```
pkg update -y
pkg install git zip unzip -y
termux-setup-storage
```
(دستور آخر یه مجوز دسترسی به فایل‌های گوشی می‌خواد، قبول کن)

## قدم ۳ — ساخت ریپازیتوری خالی روی گیت‌هاب
1. با مرورگر گوشی برو github.com و اگه اکانت نداری بساز
2. دکمه "New repository" بزن
3. اسمش رو بذار مثلاً `oxeron-vpn-app`
4. **Public** یا **Private** — هر دو کار می‌کنه (Private امن‌تره چون کد شامل ساختار اتصال به پنلته)
5. Create repository بزن (بدون اضافه کردن README یا gitignore)

## قدم ۴ — آپلود پروژه از Termux
فایل `oxeron-vpn-clone.zip` رو که از چت دانلود کردی، مطمئن شو توی پوشه Download گوشیه.
بعد توی Termux:
```
cd ~/storage/downloads
unzip oxeron-vpn-clone.zip
cd oxeron-clone
git init
git add .
git commit -m "initial commit"
git branch -M main
git remote add origin https://github.com/USERNAME/oxeron-vpn-app.git
git push -u origin main
```
به جای `USERNAME` یوزرنیم گیت‌هابتو بذار. موقع push ازت یوزرنیم و
**Personal Access Token** (نه پسورد معمولی) می‌خواد:
- توی گیت‌هاب برو: Settings → Developer settings → Personal access tokens → Generate new token (classic)
- فقط تیک `repo` رو بزن، بسازش، و همون رشته رو به‌جای پسورد وارد کن

## قدم ۵ — دیدن ساخته‌شدن APK
1. توی مرورگر برو به ریپازیتوریت روی گیت‌هاب
2. تب **Actions** رو باز کن
3. باید یه ران به اسم "Build APK" در حال اجرا یا تموم‌شده ببینی (حدود ۵-۱۰ دقیقه طول می‌کشه)
4. وقتی سبز شد (✅)، روش کلیک کن، پایین صفحه بخش **Artifacts** رو ببین
5. `oxeron-vpn-debug-apk` رو دانلود کن (یه zip حاوی APK داخلشه)
6. اکسترکتش کن، فایل `app-debug.apk` رو باز کن تا نصب بشه
   (ممکنه اندروید بگه "نصب از منابع ناشناس" رو فعال کن — با مرورگرت که فایل رو دانلود کردی این تنظیم رو بده)

## اگه خطا خورد
اگه Build توی Actions قرمز (❌) شد، روی همون ران کلیک کن، لاگ خطا رو کپی کن و برام بفرست
تا دقیق بگم مشکل کجاست — معمولاً یا یه وابستگی گم‌شده‌ست یا یه تایپوی کوچیک توی کد.

## نکته امنیتی
اگه ریپازیتوری رو Public گذاشتی، حواست باشه هیچ‌وقت یوزر/پسورد واقعی ادمین پنل
رو مستقیم توی کد ننویسی و commit نکنی — چون این فقط سورس اپه، خودِ اطلاعات
لاگین رو موقع استفاده از اپ روی گوشیت وارد می‌کنی، نه داخل فایل‌های پروژه.
